{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/day1",
            "preLaunchTask": "rust: cargo build",
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}
